---
to: src/components/<%= h.changeCase.lower(name) %>/<%= h.changeCase.lower(name) %>.styles.ts
---
import { styled } from '~/modules';

export const Wrapper = styled.div``;

export const Text = styled.p``;
